// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
	"toucan-leaderboard/app/service-main/internal/biz"
	"toucan-leaderboard/app/service-main/internal/conf"
	"toucan-leaderboard/app/service-main/internal/data"
	"toucan-leaderboard/app/service-main/internal/server"
	"toucan-leaderboard/app/service-main/internal/service"
)

// Injectors from wire.go:

// initApp init kratos application.
func initApp(confServer *conf.Server, confData *conf.Data, logger log.Logger) (*kratos.App, func(), error) {
	dataData, cleanup, err := data.NewData(confData)
	if err != nil {
		return nil, nil, err
	}
	mainRepo := data.NewMainRepo(dataData)
	mainUseCase := biz.NewMainUseCase(mainRepo)
	mainService := service.NewMainService(mainUseCase, dataData)
	httpServer := server.NewHTTPServer(confServer, mainService, logger)
	grpcServer := server.NewGRPCServer(confServer, mainService, logger)
	app := newApp(httpServer, grpcServer)
	return app, func() {
		cleanup()
	}, nil
}
